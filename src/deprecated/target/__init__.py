import os

from src import competition_path

standards_path = os.path.join(competition_path, 'standard_targets')
entities_path = os.path.join(standards_path, 'entity')
attributes_path = os.path.join(standards_path, 'attribute')
